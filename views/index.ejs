<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Streamlabs | Home</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles/style.css" />
    <script src="/javascript/index.js" defer></script>
  </head>

  <body class="bg-dark container">
    <%- include('partials/header.ejs') %>
    <main>
      <div>
        <img
          srcset="<%= `${imageURL}w300${popularMovies[0].backdrop_path} 300w,
        ${imageURL}w780${popularMovies[0].backdrop_path} 780w,
        ${imageURL}w1280${popularMovies[0].backdrop_path} 1280w` %>"
          src="<%=`${imageURL}original$popularMovies[0].backdrop_path}` %>"
          alt=""
        />
        <img
          src="<%= `${imageURL}original${popularMovies[0].poster_path}` %>"
          alt="movie poster"
        />
      </div>
    </main>
  </body>
</html>
